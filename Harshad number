class Solution:
    def sumOfTheDigitsOfHarshadNumber(self, x): 
        temp = x
        digit_sum = 0
        
        while temp > 0:
            digit_sum += temp % 10
            temp //= 10
            
        if x % digit_sum == 0:
            return digit_sum
        else:
            return -1
