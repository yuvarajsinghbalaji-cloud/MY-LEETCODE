class Solution:
    def countBits(self, n): 
        res = []
        for i in range(n + 1):
            count = 0
            binary = format(i, 'b')
            for bit in binary:
                if bit == "1":
                    count = count + 1
            res.append(count)
        return res
